#include "bit_manipulation.h"

int main()
{
    fastio

    ios_base::sync_with_stdio(false);
    cin.tie(NULL);

    DBx(binToNum("10000"))
    DBx(onesComp(7))
    DBx(onesComp(1))
    return 0;
}
